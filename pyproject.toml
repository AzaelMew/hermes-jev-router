[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-jev-router"
version = "0.1.0"
description = "Explainable Jev model routing for Hermes Agent"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Jethro Jones"}]
dependencies = []

[tool.setuptools]
packages = ["hermes_jev_router"]

# Created by Codex GPT-6 on 2026-09-17 11:03 PDT on ombee.
